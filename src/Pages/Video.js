import React from 'react';
import Navigation from '../Components/Navigation';

const Video = () => {
    return (
        <div>
            <Navigation/>
            <h1>Vidéo</h1>
        </div>
    );
};

export default Video;